/** First-party WorkBuddy PPT Skill bundled with the local PPTD v2 route. */
import type { Context } from '@deepseek-ai/cordis';
/** Stable first-party Skill name for the self-contained market package. */
export declare const WORKBUDDY_PPT_SKILL_NAME = "workbuddy-ppt";
/**
 * Register the first-party WorkBuddy PPT Skill shipped in this package.
 * @param ctx - Cordis context that owns the Skill registry.
 * @param skillRoot - Absolute root containing the bundled SKILL.md.
 */
export declare function registerWorkBuddyPptSkill(ctx: Context, skillRoot: string): void;
//# sourceMappingURL=workbuddy-ppt-skill.d.ts.map