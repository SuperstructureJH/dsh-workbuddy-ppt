/** Model tools for the independent, declarative PPT mode. */
import type { Context } from '@deepseek-ai/cordis';
import type { OfficePptService } from './service.ts';
/** Stable routing and authoring contract for the local Kimi-compatible PPTD workflow. */
export declare const OFFICE_PPT_SCENE_PROMPT: string;
/** Register deterministic scene check and compile tools for the independent PPT mode. */
export declare function registerOfficePptSceneTools(ctx: Context, service: OfficePptService): void;
//# sourceMappingURL=ppt-scene-tools.d.ts.map