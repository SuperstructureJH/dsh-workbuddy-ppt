# 安全策略

[English](SECURITY.md) | 中文

## 支持版本

安全修复面向最新 GitHub Release。DSH 仍处于开发者预览阶段，报告需包含插件版本、DSH 版本、Node.js 版本、操作系统、执行链路（`PPT` 或 `Slides`）和安装来源。

## 报告漏洞

仓库支持 GitHub 私密漏洞报告时，请使用该入口。入口不可用时，只在公开 Issue 中写简短影响说明并申请私密沟通渠道。公开 Issue 禁止包含 API Key、凭据、私有文档、客户数据、本地路径和可直接利用的攻击载荷。

有效报告应包括：

- 受影响的入口或工具；
- 去除敏感值后的最小复现请求；
- 预期的策略或路径限制结果；
- 实际发生的文件系统、网络、子进程或浏览器副作用；
- 问题出现在默认 `PPT` 链路、可选 `Slides` 链路，还是两者均可复现。

## 安全模型

默认 `PPT` 链路使用包内 PPTD 解析器和渲染器。页面、图片和输出均限制在经过校验的本地根目录内，网络图片保持关闭，模型写操作进入 DSH 审批与审计链路。

可选 `Slides` 链路通过受治理的 DSH subprocess 服务启动操作者提供的 SlideP 与 Tencent Docs 运行时文件。本仓库不分发这些文件；操作者负责确认其许可、来源、平台兼容性和本地授权。

演示文稿工具参数不接收 Provider 凭据。生图和外部模型凭据由各自的 DSH Provider 与凭据服务管理。分享诊断信息前应移除密钥和文档内容。

## 范围外事项

- 未经修改的第三方运行时漏洞应报告给其维护方；只有插件集成扩大暴露面时才同时通知本仓库。
- 模型输出质量、暂不支持的 PPTX 保真度和视觉偏好属于产品问题；引发策略绕过、路径越界或数据暴露时进入安全范围。
- 拒绝服务报告需证明请求绕过了已经记录的字节、数量、超时或子进程限制。
