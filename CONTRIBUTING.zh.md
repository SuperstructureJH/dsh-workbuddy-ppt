# 贡献指南

[English](CONTRIBUTING.md) | 中文

贡献内容应持续区分产品意图、代码实现、自动化验证、公开发行和真实 Office 应用验收。

## 从证据开始

缺陷报告需包含：

- 插件、DSH、Node.js 和操作系统版本；
- 安装来源，以及选中的 `PPT` 或 `Slides` 链路；
- 移除私有内容后的最小提示词或 PPTD 项目；
- 完整错误回执或经过限制的日志片段；
- 预期产物状态和实际产物状态。

禁止附带凭据、私有模板、客户文档、专有运行时压缩包，以及包含敏感信息的生成文件。

## 仓库结构

- `lib/` 保存预构建的 Host、Web 客户端、PPTD、装配和 invariant 发行产物。
- `skills/` 保存随包发布的模型工作流和具有再分发授权的视觉参考。
- `scripts/` 保存可由普通 Node 执行的装配入口。
- `cordis.patch.yml` 是可安装的 DSH profile 层。
- `docs/` 保存操作者和维护者文档。

本仓库是公开发行快照。运行时代码先在 WorkBuddy 源码工作区完成修改、测试和构建，再把对应预构建文件同步到本仓库。维护者不应直接手改生成的 `lib/` 文件。运行时改动应先建立 Issue，使源码变更、测试、构建和发行快照保持可追踪。

## 变更要求

- 默认 `PPT` 链路保持自包含，不依赖腾讯 SlideP、`editor_sdk` 或本机 WorkBuddy 状态。
- 可选运行时文件和凭据始终位于公开包之外。
- 保留工作区路径限制、审批、审计和原子发布。
- 行为变化时同步更新中英文文档。
- 用户可见的发行变化写入变更记录。
- 在变更影响的证据层补充验证结果。

## Commit 与 Pull Request 范围

每个 commit 应归属一个可独立审阅的行为、文档、验证变化或发行操作。Pull Request 应说明：

1. 问题和受影响链路；
2. 实现或文档变化；
3. 实际执行的命令和结果；
4. 尚未验证的环境与原生应用门禁；
5. 对安装包、npm、GitHub Release 和市场收录的影响。
