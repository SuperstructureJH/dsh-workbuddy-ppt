# Installable experimental Office PPT bundle. The feature remains outside the
# shipped Web roster until a profile explicitly adds this package.
- insert:
    - id: workbuddy-ppt
      name: 'dsh-workbuddy-ppt'
      config:
        root: !!js dshHomePath('office-ppt')
